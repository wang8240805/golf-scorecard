<view class="container">
  <!-- 数据管理 -->
  <view class="card">
    <view class="section-title">数据管理</view>
    <view class="setting-list">
      <view class="setting-item" bindtap="clearHistory">
        <text class="setting-label">清除历史比赛记录</text>
        <view class="setting-value">
          <text class="setting-text">{{gameCount}} 场</text>
          <text class="setting-arrow">›</text>
        </view>
      </view>
      <view class="setting-item" bindtap="exportData">
        <text class="setting-label">备份数据</text>
        <text class="setting-arrow">›</text>
      </view>
      <view class="setting-item" bindtap="importData">
        <text class="setting-label">恢复数据</text>
        <text class="setting-arrow">›</text>
      </view>
      <view class="setting-item" bindtap="clearAllData">
        <text class="setting-label text-danger">清除所有数据</text>
        <text class="setting-arrow">›</text>
      </view>
    </view>
  </view>

  <!-- 开发调试 -->
  <view class="card" wx:if="{{isDevtools || isDeveloperMode}}">
    <view class="section-title">{{isDeveloperMode ? '管理员功能' : '开发调试'}}</view>
    <view class="setting-list">
      <view class="setting-item" wx:if="{{isDevtools}}">
        <view class="setting-copy">
          <text class="setting-label">启动时模拟新用户</text>
          <text class="setting-desc">每次重新编译后清空本地状态，方便测试首次流程</text>
        </view>
        <switch class="setting-switch" checked="{{devAlwaysFreshUser}}" bindchange="toggleDevAlwaysFreshUser" />
      </view>
      <view class="setting-item" bindtap="resetFirstUseExperience" wx:if="{{isDevtools}}">
        <view class="setting-copy">
          <text class="setting-label">重置新用户体验</text>
          <text class="setting-desc">立即清除本地登录、隐私同意和当前比赛</text>
        </view>
        <text class="setting-arrow">›</text>
      </view>
      <view class="setting-item" bindtap="importOfficialCourses" wx:if="{{isDeveloperMode}}">
        <text class="setting-label">导入官方球场数据</text>
        <view class="setting-value">
          <text class="setting-text">{{courseCount}} 个</text>
          <text class="setting-arrow">›</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 关于 -->
  <view class="card">
    <view class="section-title">关于</view>
    <view class="about-content">
      <text class="app-name">WinPAR</text>
      <text class="app-version">版本 1.0.0</text>
      <text class="app-desc">高尔夫智能记分工具，支持多人比赛、AI分析报告和专业技术统计。</text>
    </view>
    <view class="setting-list">
      <view class="setting-item" bindtap="goToUserAgreement">
        <text class="setting-label">用户协议</text>
        <text class="setting-arrow">›</text>
      </view>
      <view class="setting-item" bindtap="goToPrivacyPolicy">
        <text class="setting-label">隐私政策</text>
        <text class="setting-arrow">›</text>
      </view>
      <view class="setting-item" bindtap="goToFeedback">
        <text class="setting-label">意见反馈</text>
        <text class="setting-arrow">›</text>
      </view>
    </view>
  </view>

</view>
